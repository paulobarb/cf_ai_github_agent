# AI Prompting Log

---

## Phase 1: Project Scoping & Planning

**Goal:** Create an optimal schedule and roadmap for my project.

**Prompt Used:**

> "Act as an experienced Technical Project Manager and Agile Scrum Master.
> I need you to create a detailed, phased project schedule based on the README file.
> Instructions:
> Analyze the provided README to understand the project's scope, features, and the specific tech stack involved.
> Break the project down into logical phases (e.g., Planning & Setup, Frontend/Backend Development, Testing, Deployment).
> For each phase, list the specific tasks required based on the tech stack mentioned.
> Assume the total project duration is [Insert Timeframe, e.g., 4 weeks / 3 months]. Distribute the time realistically across the phases."

---

## Phase 2: Foundation & Core Setup

**Goal:** Implement the specialized Security Agent.

**Prompt Used:**

> Completely rewrite src/server.ts to implement the ChatAgent (extending Agent or McpAgent) with a getGitHubPRDiff tool and a system prompt focused on OWASP security reviews and Cloudflare WAF rule generation.

---

**Goal:** Implement the specialized Security Agent.

**Prompt Used:**

> I'm trying to build the server.ts to act as the middleman between the user, AI and GitHub.
> The probem is I am getting a 'disconnected' UI and a terminal error: 'Error: Invalid transport type. McpAgent must be addressed with a valid protocol.' >
> I suspect the routeAgentRequest in my export default fetch isn't correctly handling the handshake or my frontend isn't initiating the WebSocket properly. Please:
> Check my src/server.ts entry point and ensure the fetch handler is correctly routing Agent requests.
> Verify that the ChatAgent class is correctly extending McpAgent.

---

**Goal:** Better UI response formatter

**Prompt Used:**

> look at the frontend in 'src/app.tsx'. When the AI sends a response, it includes Markdown formatting like double asterisks for bolding, but the UI is just rendering the raw text with the asterisks visible. Can you fix the message display so that it actually renders as rich text (bold, lists, etc.)?

---

**Goal:** Better UI

**Prompt Used:**

> Polish more the frontend UI in src/app.tsx by adding code syntax highlighting and styling. First, install react-syntax-highlighter and @types/react-syntax-highlighter. Next, update src/app.tsx to import the Prism highlighter (along with a dark theme like vscDarkPlus). Modify the <ReactMarkdown> component to use the components prop so that any code blocks generated by the AI are rendered using the syntax highlighter. Finally, add CSS classes to style the agent 'thinking' or system messages so they appear italicized and in a muted gray text color, distinguishing them from the final security report.

---

## Phase 3: Final Steps

**Goal:** Clean Code

**Prompt Used:**

> The codebase is currently messy. I want you to refactor this code applying strict 'Separation of Concerns'.
> Without changing any of the core business logic or features, please reorganize my code based on these architectural goals:
>
> 1.  Extract reusable UI elements into separate frontend components.
> 2.  Move all hardcoded variables and prompts into a central configuration file.
> 3.  Centralize all TypeScript interfaces and types into a shared file.
> 4.  Ensure the main application files (like App and Server) only handle routing, state, and layout.

---

**Goal:** Final READme

**Prompt Used:**

> Based on everything we did untill now, please improve my readme.

---

**Goal:** Linter compliance and optimization for sanity-check.yml

**Prompt Used:**

> Fix the linting errors. Rules: 1. Rename unused variables/parameters to start with an underscore (e.g., \_node). 2. Replace all any types with specific types or unknown. Keep all logic the same.
